!!! 5
%html(lang="en-US")
  %body
    %p You are invited by #{User.where(id: @invitation.inviter_id).first.full_name} to join MyFlix!
    %p= @invitation.message
    %p link_to "Accept this invitation", '#'
